var element = document.getElementById('pull-to-action');

suite('Element pull-to-action', function() {
    
    // Test to see if the element is building properly
    test('Element is undefined', function() {
        assert.isDefined(element, "Element pull-to-action should not be undefined");
    });
    
});
